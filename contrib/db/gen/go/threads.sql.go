// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: threads.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createThread = `-- name: CreateThread :exec
INSERT INTO agents.threads (id, user_id, last_message_pos, created_at)
VALUES ($1, $2, 0, NOW())
`

type CreateThreadParams struct {
	ID     string
	UserID uuid.UUID
}

// CreateThread creates a new chat thread (Aggregate Root in DDD terms).
// Initializes last_message_pos to 0, which serves as the version counter
// for Optimistic Concurrency Control when appending messages.
//
// Usage: Call this once when starting a new conversation.
func (q *Queries) CreateThread(ctx context.Context, arg CreateThreadParams) error {
	_, err := q.db.Exec(ctx, createThread, arg.ID, arg.UserID)
	return err
}

const getThreadWithMessages = `-- name: GetThreadWithMessages :many
SELECT
    t.id AS thread_id,
    t.user_id,
    t.last_message_pos,
    t.created_at AS thread_created_at,
    m.position,
    m.msg_type,
    m.merge_tag,
    m.created_at AS message_created_at,
    u.content AS user_content,
    a.text AS assistant_text,
    a.reasoning AS assistant_reasoning,
    a.agent_id::UUID AS assistant_agent_id,
    tr.tool_id::UUID AS req_tool_id,
    tr.tool_name AS req_tool_name,
    tr.tool_call_id AS req_tool_call_id,
    tr.reasoning AS req_reasoning,
    tr.arguments AS req_arguments,
    ts.request_position AS result_request_position,
    ts.tool_call_id AS result_tool_call_id,
    ts.is_error AS result_is_error,
    ts.content AS result_content,
    tr_origin.tool_name AS result_tool_name
FROM agents.threads t
LEFT JOIN agents.messages m ON t.id = m.thread_id
LEFT JOIN agents.messages_user u
    ON m.thread_id = u.thread_id AND m.position = u.position
LEFT JOIN agents.messages_assistant a
    ON m.thread_id = a.thread_id AND m.position = a.position
LEFT JOIN agents.messages_tool_request tr
    ON m.thread_id = tr.thread_id AND m.position = tr.position
LEFT JOIN agents.messages_tool_result ts
    ON m.thread_id = ts.thread_id AND m.position = ts.position
LEFT JOIN agents.messages_tool_request tr_origin
    ON ts.thread_id = tr_origin.thread_id AND ts.request_position = tr_origin.position
WHERE t.id = $1
ORDER BY m.position ASC NULLS FIRST
`

type GetThreadWithMessagesRow struct {
	ThreadID              string
	UserID                uuid.UUID
	LastMessagePos        int64
	ThreadCreatedAt       pgtype.Timestamptz
	Position              *int64
	MsgType               *string
	MergeTag              *int64
	MessageCreatedAt      pgtype.Timestamptz
	UserContent           *string
	AssistantText         *string
	AssistantReasoning    *string
	AssistantAgentID      uuid.UUID
	ReqToolID             uuid.UUID
	ReqToolName           *string
	ReqToolCallID         *string
	ReqReasoning          *string
	ReqArguments          []byte
	ResultRequestPosition *int64
	ResultToolCallID      *string
	ResultIsError         *bool
	ResultContent         []byte
	ResultToolName        *string
}

// GetThreadWithMessages retrieves thread metadata AND all messages in a single query.
// This combines what were previously GetThread + GetThreadMessages for efficiency.
//
// Returns:
//
//	First row contains thread metadata (id, last_message_pos, created_at)
//	Subsequent rows contain messages with type-specific data via LEFT JOINs
//
// The last_message_pos field is critical for Optimistic Concurrency Control.
// If the thread has no messages, returns a single row with thread metadata
// and NULL message fields.
//
// Usage: Call when loading a chat thread. Check last_message_pos before
// adding new messages to implement OCC.
func (q *Queries) GetThreadWithMessages(ctx context.Context, id string) ([]GetThreadWithMessagesRow, error) {
	rows, err := q.db.Query(ctx, getThreadWithMessages, id)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GetThreadWithMessagesRow
	for rows.Next() {
		var i GetThreadWithMessagesRow
		if err := rows.Scan(
			&i.ThreadID,
			&i.UserID,
			&i.LastMessagePos,
			&i.ThreadCreatedAt,
			&i.Position,
			&i.MsgType,
			&i.MergeTag,
			&i.MessageCreatedAt,
			&i.UserContent,
			&i.AssistantText,
			&i.AssistantReasoning,
			&i.AssistantAgentID,
			&i.ReqToolID,
			&i.ReqToolName,
			&i.ReqToolCallID,
			&i.ReqReasoning,
			&i.ReqArguments,
			&i.ResultRequestPosition,
			&i.ResultToolCallID,
			&i.ResultIsError,
			&i.ResultContent,
			&i.ResultToolName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getToolRequestPosition = `-- name: GetToolRequestPosition :one
SELECT position FROM agents.messages_tool_request
WHERE thread_id = $1 AND tool_call_id = $2
`

type GetToolRequestPositionParams struct {
	ThreadID   string
	ToolCallID string
}

func (q *Queries) GetToolRequestPosition(ctx context.Context, arg GetToolRequestPositionParams) (int64, error) {
	row := q.db.QueryRow(ctx, getToolRequestPosition, arg.ThreadID, arg.ToolCallID)
	var position int64
	err := row.Scan(&position)
	return position, err
}

const insertMessageAssistant = `-- name: InsertMessageAssistant :execrows
WITH update_thread AS (
    UPDATE agents.threads
    SET last_message_pos = $4::BIGINT
    WHERE id = $5::TEXT AND last_message_pos = $6::BIGINT
    RETURNING id
),
msg AS (
    INSERT INTO agents.messages (thread_id, position, msg_type, merge_tag, created_at)
    SELECT $5::TEXT, $4::BIGINT, 'assistant', $7::BIGINT, NOW()
    FROM update_thread
    RETURNING thread_id, position
)
INSERT INTO agents.messages_assistant (thread_id, position, text, reasoning, agent_id)
SELECT thread_id, position, $1, $2, $3::UUID FROM msg
`

type InsertMessageAssistantParams struct {
	Text                  string
	Reasoning             string
	AgentID               uuid.UUID
	Position              int64
	ThreadID              string
	CurrentLastMessagePos int64
	MergeTag              int64
}

// InsertMessageAssistant atomically inserts an assistant message and updates thread position.
// Uses CTEs to perform all operations in a single SQL statement with OCC.
//
// Parameters:
//
//	thread_id
//	position (must equal current last_message_pos + 1)
//	current_last_message_pos (for OCC check)
//	merge_tag
//	text (assistant's response, may be empty if reasoning is present)
//	reasoning (chain-of-thought, may be empty if text is present)
//	agent_id (reference to agent_settings)
//
// Note: CHECK constraint ensures at least one of (text, reasoning) is non-empty.
// This allows the model to "think" without necessarily responding.
func (q *Queries) InsertMessageAssistant(ctx context.Context, arg InsertMessageAssistantParams) (int64, error) {
	result, err := q.db.Exec(ctx, insertMessageAssistant,
		arg.Text,
		arg.Reasoning,
		arg.AgentID,
		arg.Position,
		arg.ThreadID,
		arg.CurrentLastMessagePos,
		arg.MergeTag,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const insertMessageToolRequest = `-- name: InsertMessageToolRequest :execrows
WITH update_thread AS (
    UPDATE agents.threads
    SET last_message_pos = $6::BIGINT
    WHERE id = $7::TEXT AND last_message_pos = $8::BIGINT
    RETURNING id
),
msg AS (
    INSERT INTO agents.messages (thread_id, position, msg_type, merge_tag, created_at)
    SELECT $7::TEXT, $6::BIGINT, 'tool_request', $9::BIGINT, NOW()
    FROM update_thread
    RETURNING thread_id, position
)
INSERT INTO agents.messages_tool_request (thread_id, position, tool_id, tool_name, tool_call_id, reasoning, arguments)
SELECT thread_id, position, $1::UUID, $2, $3, $4, $5 FROM msg
`

type InsertMessageToolRequestParams struct {
	ToolID                pgtype.UUID
	ToolName              string
	ToolCallID            string
	Reasoning             string
	Arguments             []byte
	Position              int64
	ThreadID              string
	CurrentLastMessagePos int64
	MergeTag              int64
}

// InsertMessageToolRequest atomically inserts a tool request and updates thread position.
// Uses CTEs to perform all operations in a single SQL statement with OCC.
//
// Parameters:
//
//	thread_id
//	position
//	current_last_message_pos (for OCC)
//	merge_tag
//	tool_id (FK to mcp_tools, must never be inserted as null)
//	tool_name (denormalized - preserves historical name even if tool renamed)
//	tool_call_id (unique identifier for matching with result)
//	reasoning (model's thinking before calling tool)
//	arguments (JSONB object with tool parameters)
//
// Note: tool_name is intentionally denormalized to create an immutable
// historical record of the exact tool name at invocation time.
func (q *Queries) InsertMessageToolRequest(ctx context.Context, arg InsertMessageToolRequestParams) (int64, error) {
	result, err := q.db.Exec(ctx, insertMessageToolRequest,
		arg.ToolID,
		arg.ToolName,
		arg.ToolCallID,
		arg.Reasoning,
		arg.Arguments,
		arg.Position,
		arg.ThreadID,
		arg.CurrentLastMessagePos,
		arg.MergeTag,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const insertMessageToolResult = `-- name: InsertMessageToolResult :execrows
WITH update_thread AS (
    UPDATE agents.threads
    SET last_message_pos = $5::BIGINT
    WHERE id = $6::TEXT AND last_message_pos = $7::BIGINT
    RETURNING id
),
msg AS (
    INSERT INTO agents.messages (thread_id, position, msg_type, merge_tag, created_at)
    SELECT $6::TEXT, $5::BIGINT, 'tool_result', $8::BIGINT, NOW()
    FROM update_thread
    RETURNING thread_id, position
)
INSERT INTO agents.messages_tool_result (thread_id, position, request_position, tool_call_id, is_error, content)
SELECT thread_id, position, $1::BIGINT, $2, $3, $4 FROM msg
`

type InsertMessageToolResultParams struct {
	RequestPosition       int64
	ToolCallID            string
	IsError               bool
	Content               []byte
	Position              int64
	ThreadID              string
	CurrentLastMessagePos int64
	MergeTag              int64
}

// InsertMessageToolResult atomically inserts a tool execution result and updates thread position.
// Uses CTEs to perform all operations in a single SQL statement with OCC.
//
// Parameters:
//
//	thread_id
//	position (this message's position, NOT the request position)
//	current_last_message_pos (for OCC)
//	merge_tag
//	request_position (FK to the original tool_request message)
//	tool_call_id (must match the request's tool_call_id)
//	is_error (false for success, true for errors)
//	content (JSONB - either success result or error details)
//
// The request_position FK with ON DELETE CASCADE ensures referential integrity.
func (q *Queries) InsertMessageToolResult(ctx context.Context, arg InsertMessageToolResultParams) (int64, error) {
	result, err := q.db.Exec(ctx, insertMessageToolResult,
		arg.RequestPosition,
		arg.ToolCallID,
		arg.IsError,
		arg.Content,
		arg.Position,
		arg.ThreadID,
		arg.CurrentLastMessagePos,
		arg.MergeTag,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const insertMessageUser = `-- name: InsertMessageUser :execrows
WITH update_thread AS (
    UPDATE agents.threads
    SET last_message_pos = $2::BIGINT
    WHERE id = $3::TEXT AND last_message_pos = $4::BIGINT
    RETURNING id
),
msg AS (
    INSERT INTO agents.messages (thread_id, position, msg_type, merge_tag, created_at)
    SELECT $3::TEXT, $2::BIGINT, 'user', $5::BIGINT, NOW()
    FROM update_thread
    RETURNING thread_id, position
)
INSERT INTO agents.messages_user (thread_id, position, content)
SELECT thread_id, position, $1 FROM msg
`

type InsertMessageUserParams struct {
	Content               string
	Position              int64
	ThreadID              string
	CurrentLastMessagePos int64
	MergeTag              int64
}

// InsertMessageUser atomically inserts a user message and updates thread position.
// Uses CTEs to perform UPDATE (with OCC check), then INSERT into messages and messages_user
// in a SINGLE SQL statement.
//
// Parameters:
//
//	thread_id
//	position (must equal current last_message_pos + 1)
//	current_last_message_pos (for OCC check in UPDATE)
//	merge_tag (for grouping related messages, typically 0)
//	content (user's message text)
//
// If the UPDATE affects 0 rows (OCC conflict), the entire statement does nothing
// and returns 0 rows - caller must check RowsAffected and retry if needed.
//
// Transaction flow (all in one statement):
//  1. UPDATE threads with OCC check (if fails, stop here)
//  2. INSERT into messages (base table)
//  3. INSERT into messages_user (type-specific)
//
// Usage: Single atomic operation to insert user message with OCC.
func (q *Queries) InsertMessageUser(ctx context.Context, arg InsertMessageUserParams) (int64, error) {
	result, err := q.db.Exec(ctx, insertMessageUser,
		arg.Content,
		arg.Position,
		arg.ThreadID,
		arg.CurrentLastMessagePos,
		arg.MergeTag,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
