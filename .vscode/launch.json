{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "cynosure",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/cynosure",
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}
