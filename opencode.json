{
    "$schema": "https://opencode.ai/config.json",
    "tools": {
        // for now, replace tool works incorrectly, and it's unsafe to allow
        // models to use this tool. However, we are still prototyping tools for
        // agents, so it's better to leave it even not working properly.
        "replace": false
    }
}
